DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=carbon-mice
SEED_SUPERADMIN_EMAIL=
SEED_SUPERADMIN_NAME=
SEED_SUPERADMIN_PASSWORD=

# Background jobs / session cleanup (Solid Queue)
# SOLID_QUEUE_IN_PUMA=true   # production: run the Solid Queue supervisor inside Puma
# JOB_CONCURRENCY=1          # Solid Queue worker processes
# ADMIN_SESSION_TTL_DAYS=30  # sessions older than this are purged by PurgeSessionsJob
